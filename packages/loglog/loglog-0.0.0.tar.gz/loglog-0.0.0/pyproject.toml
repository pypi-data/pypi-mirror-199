[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "loglog"
version = "0.0.0"
authors = [
  { name="hwelsters", email="redacted@redacted.redacted" },
]
description = "A small, charming and pretty logging library"
readme = "README.md"
requires-python = ">=3.7"
classifiers = []
dependencies = [
  'openai >= 0.27.2',
  'fasteners >= 0.18'
]

[project.urls]
"Homepage" = "https://github.com/hwelsters/loglog"
"Bug Tracker" = "https://github.com/hwelsters/loglog/issues"
