[tool.poetry]
name = "fastflexray"
version = "0.1.0"
description = ""
authors = ["wanqiang.liu <919740574@qq.com>"]
readme = "README.md"

[tool.poetry.scripts]
fastflexray = "fastflexray.__main__:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.7.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
