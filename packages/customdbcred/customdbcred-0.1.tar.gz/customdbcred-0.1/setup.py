from setuptools import setup, find_packages

setup(
    name='customdbcred',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        #list any dependencies

    ],
)
