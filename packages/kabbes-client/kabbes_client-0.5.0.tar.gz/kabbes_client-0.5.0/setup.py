from setuptools import setup

if __name__ == '__main__':
    setup(
        package_data={'kabbes_client': 
            [ 
                'Package.json',
                'Root.json' 
            ]
            }
    )