[tool.poetry]
name = "mm_poetry"
version = "0.1.1"
description = "Poetry project"
authors = ["AC <ac@marketmining.com.br>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pendulum = "^2.1.2"

[tool.poetry.group.test.dependencies]
pytest = "^7.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
