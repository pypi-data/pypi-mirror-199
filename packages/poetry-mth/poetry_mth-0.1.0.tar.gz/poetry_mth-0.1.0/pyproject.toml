[tool.poetry]
name = "poetry-mth"
version = "0.1.0"
description = ""
authors = ["Python Powerhouse"]
readme = "README.md"
packages = [{include = "poetry_mth"}]

[tool.poetry.dependencies]
python = "^3.10"
prettytable = "^3.6.0"
prompt-toolkit = "^3.0.38"
pygame = "^2.3.0"

[tool.poetry.scripts]
mth = "poetry_mth.main.py:run"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
