[tool.poetry]
name = "uwcip-sharedutils"
version = "0.1.0"
description = ""
authors = ["Lia Bozarth <liafan@uw.edu>"]
readme = "README.md"
packages = [{include = "uwcip/sharedutils"}]

[tool.poetry.dependencies]
python = ">=3.9"
pyyaml = "^6.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
