import setuptools

setuptools.setup(
    name="Liblabtest",
    version="1.0.4",

    license='SEE LICENSE IN LICENSE',

    packages=setuptools.find_packages(),

    install_requires=[
      'requests',
    ]
)
