[tool.poetry]
name = "pod-gpt"
version = "0.0.1"
description = "Package for building podcast search indexes."
authors = ["James Briggs <james@aurelio.ai>"]
readme = "README.md"
packages = [{include = "pod_gpt"}]

[tool.poetry.dependencies]
python = "^3.9"
langchain = "^0.0.125"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
