[tool.poetry]
name = "auve"
version = "23.3.1"
description = "Simple tool for automatic version number generation"
authors = ["Marc Scoop <marc.scoop@bitmarc.org>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
