from setuptools import setup, find_packages

setup(
    name ='pystarpix',
    version = '1.0.0',
    description = 'only star pix',
    author = 'crandi',
    author_email = None,
    url = None,
    install_requires = [],
    packages=find_packages(),
)
