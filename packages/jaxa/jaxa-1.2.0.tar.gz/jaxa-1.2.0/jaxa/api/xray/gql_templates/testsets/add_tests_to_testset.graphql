mutation addTestToTestSet ($testset_id: String!, $test_ids: [String]!) {
    addTestsToTestSet(
        issueId: $testset_id,
        testIssueIds: $test_ids
    ) {
        addedTests
        warning
    }
}
