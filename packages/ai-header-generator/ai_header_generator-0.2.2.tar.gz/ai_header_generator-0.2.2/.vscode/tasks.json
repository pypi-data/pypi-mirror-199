{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy to PyPI",
            "type": "shell",
            "command": "${workspaceFolder}/pypiUpload.bat",
            "args": ["${fileDirname}/setup.py", "--verbose"],
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}