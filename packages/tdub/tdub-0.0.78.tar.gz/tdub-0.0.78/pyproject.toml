[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "setuptools_scm[toml]>3.4"
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/tdub/version.py"

[tool.black]
line-length = 88
target-version = ["py38"]
extend-exclude = '''
__main__.py
'''
