[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "popsims"
version = "0.2.3"
dependencies = [
    "astropy",
    "scipy",
    "matplotlib",
    "numpy",
    "numba",
    "pandas",
    "tqdm",
    "seaborn",
]
