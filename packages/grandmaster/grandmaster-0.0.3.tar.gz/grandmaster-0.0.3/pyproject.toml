[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "grandmaster"
version = "0.0.3"
readme = "README.md"

dependencies = [
  "torch==2.0.0",
  "transformers==4.27.3",
  "pillow==9.4.0",
  "datasets==2.10.1",
  "jinja2==3.1.2",
  "black==23.1.0"
]
